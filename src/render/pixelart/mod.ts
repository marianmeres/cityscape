/**
 * `@marianmeres/cityscape/render/pixelart` — the pixel-art Canvas2D {@link Renderer}.
 *
 * A low-resolution, palette-quantised, ordered-dithered target for the same display list the
 * Canvas and ASCII renderers consume. A third proof that the seam is real.
 *
 * @module
 */

export { type PixelArtOptions, PixelArtRenderer } from "./pixelart-renderer.ts";
